## Hermes 桌宠 v1.4.7

### 针对 macOS 27 优化

如果你升级到了 **macOS 27**，可能会遇到「在线 AI」用不了——发消息时提示**「连接断开 / opencode 子进程在 ready 之前就退出了」**。这版专门修了这个问题。

**这版做了什么**

- **修好 macOS 27 上「在线 AI 连接断开」**：在线 AI 内置的引擎（opencode）是一个独立的可执行程序，本地安装时它的代码签名方式在旧系统能跑、但 **macOS 27 收紧校验后会被系统直接拦截**，导致引擎刚启动就被杀掉、连不上。这版改正了签名方式（单独、正确地给引擎签名），macOS 27 上在线 AI 恢复正常。
  - 其它对话模式（Claude Code / Codex / Hermes / QwenCode）不受此问题影响。
- **错误更透明**：万一引擎启动失败，现在会把**真正的失败原因**显示出来（之前这部分信息被丢弃了，排查只能靠系统崩溃日志），以后这类问题一眼能看明白。

> 说明：通过官方 DMG 安装的版本本身没有这个签名问题；主要影响的是 macOS 27 升级用户。给受影响的朋友带来的不便很抱歉。

### 下载安装

按你的 Mac 芯片选对应的版本：

- **Apple Silicon**（M1 / M2 / M3 / M4 系列）→ 下载 `HermesPet-1.4.7-AppleSilicon.dmg`
- **Intel 芯片** → 下载 `HermesPet-1.4.7-Intel.dmg`
- 不确定？点屏幕左上角  菜单 →「关于本机」，看「芯片」那一行。

安装步骤：

1. 打开下载好的 DMG
2. 把「Hermes 桌宠」拖进「应用程序」文件夹
3. 从启动台 / Spotlight 打开「Hermes 桌宠」即可

老用户无需手动操作，App 会在 24 小时内自动检测到新版本并提示更新（也可手动：设置 → 检查更新）。
